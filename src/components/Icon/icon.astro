---
import { Icon as AstroIcon } from "astro-icon/components";
interface Props {
  name: string;
  width?: number;
  height?: number;
  size?: number;
}

const { name, width, height, size = 24 } = Astro.props;
---

<AstroIcon name={name} width={width} height={height} size={size} />
